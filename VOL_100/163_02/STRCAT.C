/*
** append s2 to the end of s1
*/
strcat(s1, s2) char *s1, *s2; {
  char *strcat;
  strcat = s1;
  while(*s1) s1++;
  while(*s1++ = *s2++);
  return strcat;
  }
