/*
** compare s1 and s2
** result is <, ==, or > 0 as s1 is <, ==, > s2
*/
strcmp(s1, s2) char *s1, *s2; {
  while(1) {
    if(*s1 > *s2) return 1;
    if(*s1 < *s2++) return -1;
    if(!*s1++) return 0;
    }
  return 0;
  }
