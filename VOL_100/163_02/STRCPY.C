/*
** copy s2 to s1
*/
strcpy(s1, s2) char *s1, *s2; {
  char *strcpy;
  strcpy = s1;
  while(*s1++ = *s2++);
  return strcpy;
  }
