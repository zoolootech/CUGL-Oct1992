/*
** if character is upper case, convert to lower case
*/
tolower(c) char c; {
  if(c >= 'A' && c <= 'Z') return (c + ('a' - 'A'));
  return c;
  }
