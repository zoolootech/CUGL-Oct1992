/*
** clear error on stream
*/
#define FILE int
#include "streamio.h"
 
clearerr(stream) FILE *stream; {
  stream[_IOB_FLAG] &= ~_ERR;
  }
