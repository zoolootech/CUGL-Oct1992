/*
** rewind a stream file
*/
#define FILE int
extern fseek2();
 
rewind(stream) FILE *stream; {
  fseek2(stream, 0, 0);
  fseek2(stream, 0, 3);
  }
