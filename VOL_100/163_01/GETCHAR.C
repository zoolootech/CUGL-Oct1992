/*
** get a character from "stdin"
*/
extern int *stdin, fgetc();
 
getchar() {
  return fgetc(stdin);
  }
