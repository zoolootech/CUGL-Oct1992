/*
** islower -- true if argument is valid ASCII lower case alphabetic
*/
islower(c) char c; {
  if(c >= 'a' && c <= 'z') return 1;
  else return 0;
  }
