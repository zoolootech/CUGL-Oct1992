/*
** put a character to "stdout"
*/
extern int *stdout, fputc();
 
putchar(c) char c; {
  return fputc(c, stdout);
  }
