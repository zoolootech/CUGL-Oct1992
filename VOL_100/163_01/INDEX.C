/*
** return pointer to the first occurrence of c in s
*/
index(s, c) char *s, c; {
  while(*s) if(*s++ == c) return --s;
  return 0;
  }
