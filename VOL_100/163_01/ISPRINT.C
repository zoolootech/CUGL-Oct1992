/*
** isprint -- true if argument is valid ASCII graphic
*/
isprint(c) char c; {
  if(c >= ' ' && c <= 126) return 1;
  else return 0;
  }
