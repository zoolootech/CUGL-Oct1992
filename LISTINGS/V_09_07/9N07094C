
    Display *display;
    Window  window;
    GC      gc;
    int     x, y;
    char    *string;
    int     length;

    XDrawImageString( display, window,
            gc, x, y,
            string, length );

    XDrawString( display, window,
            gc, x, y,
            string, length );

