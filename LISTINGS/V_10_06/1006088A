/* LISTING 1 */

extern int g;

f1() 
{
    int i;
    int *p, *q, *r;
    int a[200], b[200] ,c[200];

    p = a; q = b; r = c;

    for( i=0; i<g; ++i )
        *p++ = *q++ / *r++;
}
