print_hex(number, digits)
/* Prints a value in hexidecimal format */
unsigned char *number;
int digits;
        {
        printf("   Number is ");
        while (digits--)
                {
                printf(" %2x",*number++);
                }
        printf("\n");
        }

