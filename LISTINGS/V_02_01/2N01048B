


function search(var s : string) : integer;
	var
		i : integer;
		found : boolean;
	begin
	found := false;
	i := MAX;
	while (i > 0) and (not found) do
		if s = table[i] then
			found := true
		else
			i := i - 1;
	search := i;
	end;

