


{*
 * return true if c is a digit; otherwise return false.
 *}
function isdigit(c : char) : boolean;
	begin
	if ('0' <= c) and (c <= '9') then
		isdigit := true
	else
		isdigit := false;
	end;

