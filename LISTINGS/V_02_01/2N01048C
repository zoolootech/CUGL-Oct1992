


function search(var s : string) : integer;
	label
		1;
	var
		i : integer;
	begin
	i := MAX;
	while i > 0 do
		begin
		if s = table[i] then
			begin
			search := i;
			goto 1;
			end;
		i := i - 1;
		end;
	search := 0;
1:
	end;

