#include "stdio.h"

int addup(int * a, int b);

int main()
     {
     int x = 1, y = 2, z = 0;

     z = addup(&x,y);
     printf("x = %d, y = %d, z = %d",x,y,z);
     return 0;
     }

int addup(int * a, int b)
     {
     *a += b;
     return *a;
     }
