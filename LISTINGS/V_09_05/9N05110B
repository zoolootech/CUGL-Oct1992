/*
 * ln_seq.h - line number sequence interface
 */
class ln_seq
    {
public:
    ln_seq();
    ln_seq(unsigned);  // new constructor
    void add(unsigned);
    void print();
private:
    struct listnode
        {
        unsigned number;
        listnode *next;
        };
    listnode *first, *last;
    };

