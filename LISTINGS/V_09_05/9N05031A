
	#include <stdio.h>
	#include <X11/Xlib.h>

	Display	*display;
	char		*display_name;

	display = XOpenDisplay( display_name );

	if ( display != (Display *) NULL )
		{
		/* 
		 * We have success!
		 */
		}

