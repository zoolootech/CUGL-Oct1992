
Listing 1

#include <ctype.h>

int atoi(const char s[i])
	{
	int i = 0;

	while (isspace(s[i]))
		++i;
	if (s[i] == '+' || s[i] == '-')
		++i;
	while (isdigit(s[i]))
		++i;
	return 0;
	}

----------

