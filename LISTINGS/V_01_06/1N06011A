
// okay; args for every parameter
x = do_quad1(1.0,2.0,3.0);

// wrong! first parameter lacks a default value!
x = do_quad1();

// Okay; b and c have default values
x = do_quad1(6.0);

// error! parameters can only be dropped from right-to-left!
x = do_quad1(1.1,,2.0);

