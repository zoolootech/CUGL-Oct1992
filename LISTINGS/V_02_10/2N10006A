/*
 * subsq.c - 32-bit sum-of-squares function.
 *
 * Assumes ints are 32 bits.
 */

int sumsq(int n)
    {
    register int i;
    register int sum = 0;
    for (i = 1; i < n; ++i)
        sum += i * i;
    return sum;
    }

