/*
 * redirtst.c:
 *
 * Test redirection autodetection function.
 * To compile with C version (Borland C++):
 *
 *      bcc redirtst.c redir.obj
 *
 * To compile with assembly language version (Borland C++):
 *      tasm /mx redira
 *      bcc redirtst.c redira.obj
 */

unsigned char redir();

void main()
{
    printf("redir() returns (in hex): %02x\n", redir());
}
/* End of File */
