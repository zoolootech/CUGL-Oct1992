

Listing 6

#ifdef DEBUG
	#define trace(p) fprintf(stderr, #p "\n")
#else
	#define trace(p) ((void)0)
#endif

...

int find(const char *s)
	{
	trace(entering find);

	...

	trace(leaving find);
	}

