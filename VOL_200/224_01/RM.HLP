NAME
     rm - remove (unlink) files

SYNTAX
     rm [ -i ]  file ...

DESCRIPTION
     The rm command removes the specified files.  Directories and 
     read-only files can not be removed by rm.  Use rmdir to remove
     directories.  

OPTION
     -i     File removal is interactive.  The rm command asks whether to 
            delete each file name. 

SEE ALSO
     rmdir
