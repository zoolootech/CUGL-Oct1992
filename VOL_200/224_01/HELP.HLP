NAME 
    help - listing of online help files
    
SYNTAX
    help [file_list]

DESCRIPTION
    Help will list the specified files to the standard output.  The extension
    of '.hlp' will be added if no extension is given.  Help also searches the
    $PATH environment parameter to find the files.  If no file is specified,
    help.hlp is listed, and help enters a loop to read the users requests.
    Wildcards can be used in the filenames (enclose in quotes on command line).
    
    Help files available are:
    
    CAT		CECHO		CHMOD		CMP		COMM
    CTIME	EXPR		FIND		GREP		HEAD
    HOC		LS		OD		PR		REV
    RM		SORT		STRINGS		TAIL		TEE
    TEST	TR		UNIQ		UTILIX		WC
