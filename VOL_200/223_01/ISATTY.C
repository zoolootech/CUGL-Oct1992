/*
** Return "true" if fd is a device, else "false"
*/
#include <stdio.h>

isatty(fd) int fd; {
   if(fd >255)
      return(FALSE);
   else
      return(TRUE);
  }
