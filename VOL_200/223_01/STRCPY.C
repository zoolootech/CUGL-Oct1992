/*
** copy t to s 
*/
  static char *xd;

strcpy(s, t) char *s, *t; {
  xd = s;
  while (*s++ = *t++) ;
  return(xd);
  }
