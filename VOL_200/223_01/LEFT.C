/*
** left -- left adjust and null terminate a string
*/
   static char *str2;

left(str) char *str; {
  str2=str;
  while(*str2==' ') ++str2;
  while(*str++ = *str2++);
  }
