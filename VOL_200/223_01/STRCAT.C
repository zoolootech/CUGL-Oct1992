/*
** concatenate t to end of s 
** s must be large enough
*/
  static char *xd;

strcat(s, t) char *s, *t; {
  xd = s;
  --s;
  while (*++s) ;
  while (*s++ = *t++) ;
  return(xd);
  }
